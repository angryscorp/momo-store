apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: production-ingress
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/angryscorp/momo-store.git
    targetRevision: main
    path: deploy/charts/ingress
    helm:
      releaseName: production-ingress
      valueFiles:
        - ../../envs/production/ingress-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: production
