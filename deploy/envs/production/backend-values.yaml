replicaCount: 2

image:
  # Pin to a specific release tag in production. Update via PR when cutting a release.
  # Example: tag: release-2605101230-abc1234
  tag: main
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
